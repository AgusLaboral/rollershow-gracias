# rollershow-gracias

Este repo es el **build estático** de la página /gracias — NO es código fuente. La fuente vive en `Desktop\rollershow-src` (frontend-lab, Astro) y este repo solo recibe builds.

**Antes de tocar nada acá: leer el mapa de repos en `C:\Users\Agus\Desktop\rollershow\CLAUDE.md`.** Regla crítica: nunca borrar ni recrear este repo; los cambios de contenido se hacen en rollershow-src y se re-buildea.
